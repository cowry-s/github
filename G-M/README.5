# 学生成绩管理系统 (Student Score Manager)

一个基于 CSV 文件存储的命令行学生成绩管理程序。支持显示、添加、删除、修改、查询学生记录，数据以 CSV 格式持久化保存，方便查看和编辑。

## 功能特性

- **显示全部**：以表格形式列出所有学生记录
- **添加学生**：输入学号、姓名、成绩，学号不可重复，姓名不可为空
- **删除学生**：根据学号删除指定记录
- **修改学生**：根据学号修改姓名和/或成绩，留空表示保持不变
- **查询学生**：按学号或姓名关键字模糊查询
- **自动保存**：每次增删改后立即写入 CSV 文件
- **输入验证**：成绩必须为 0–100 之间的数字，无效输入会被拒绝
- **UTF-8 BOM 编码**：使用 `utf-8-sig`，Excel 直接打开中文不乱码

## 环境要求

- Python 3.6 或更高版本
- 无需安装第三方库

## 使用方法

### 直接运行脚本

将代码保存为 `student_manager.py`，然后在终端运行：

```bash
python student_manager.py
