# FizzBuzz

一个经典的 FizzBuzz 程序。从 1 数到 N，根据数字是否为 3 或 5 的倍数输出不同的内容。

## 功能特性

- 从 1 遍历到用户指定的 N
- 是 3 的倍数输出 `Fizz`
- 是 5 的倍数输出 `Buzz`
- 同时是 3 和 5 的倍数输出 `FizzBuzz`
- 既不是 3 也不是 5 的倍数则输出数字本身
- 代码简洁，使用字符串拼接实现组合输出
- 纯标准库实现，无需安装第三方依赖

## 环境要求

- Python 3.6 或更高版本
- 无需安装任何第三方库

## 使用方法

### 直接运行脚本

将代码保存为 `fizz_buzz.py`，然后在终端运行：

```bash
python fizz_buzz.py
