# FizzBuzz

一个简单的 FizzBuzz 命令行程序。  
输入一个正整数 N，程序会从 1 打印到 N，并按照 FizzBuzz 规则替换数字。

## ✨ 功能特性

- 输入 N，输出 1 到 N
- 3 的倍数输出 `Fizz`
- 5 的倍数输出 `Buzz`
- 同时是 3 和 5 的倍数输出 `FizzBuzz`
- 其他数字原样输出
- 无需第三方依赖，仅使用 Python 标准库

## 📁 项目结构

```
fizz-buzz/
├── fizz_buzz.py      # 主程序
└── README.md         # 项目说明
```

> 如果你的文件名不是 `fizz_buzz.py`，把下面命令中的文件名替换成实际文件名即可。

## 🚀 快速开始

### 环境要求

- Python 3.6 或更高版本
- 无需安装任何第三方库

### 运行方式

```bash
python fizz_buzz.py
```

运行后输入 N：

```
请输入 N: 15
```

## 📖 输出示例

输入 `15` 时，输出如下：

```
1
2
Fizz
4
Buzz
Fizz
7
8
Fizz
Buzz
11
Fizz
13
14
FizzBuzz
```

## 🧠 逻辑说明

对每个数字 `i`（从 1 到 N）：

1. 如果 `i` 能被 3 整除，输出加上 `Fizz`
2. 如果 `i` 能被 5 整除，输出加上 `Buzz`
3. 如果以上都没有命中，则输出数字 `i` 本身

因此：

| 条件 | 输出 |
|------|------|
| 3 的倍数 | Fizz |
| 5 的倍数 | Buzz |
| 同时是 3 和 5 的倍数 | FizzBuzz |
| 其他 | 数字本身 |

## ⚠️ 注意事项

- 输入应为整数。
- 如果输入非整数，程序会抛出 `ValueError`。
- 如果输入负数或 0，程序不会输出任何内容。
- 如果需要更友好的输入校验，可以自行添加 `try/except` 或循环输入。

## 🧪 可选扩展练习

- 增加输入校验，非整数时提示重新输入
- 支持命令行参数：`python fizz_buzz.py 15`
- 把结果写入文件
- 为 `fizz_buzz` 函数添加单元测试
- 支持自定义规则，例如 7 的倍数输出 `Jazz`

## 📄 许可证

本项目仅供学习使用，可自由修改和分发。
